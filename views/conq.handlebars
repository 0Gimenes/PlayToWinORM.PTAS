<h1>Cartões Cadastrados</h1>

<a href="/jogos/{{jogo.id}}/novoCartao">Cadastrar Conquista</a>

<table border="1">
  <!-- table row -->
  <tr>
    <!-- table head -->
    <th>Número</th>
    <th>Nome</th>
    <th>Nome</th>
  </tr>
  {{#each cartoes}}
    <tr>
      <td>{{this.numero}}</td>
      <td>{{this.nome}}</td>
    </tr>
  {{/each}}
</table>